/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['randomuser.me', 'api.dicebear.com', 'avatar.iran.liara.run','images.pexels.com','placeimg.com','cdn.pixabay.com'],
      },
};

export default nextConfig;
